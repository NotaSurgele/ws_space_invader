/*
** EPITECH PROJECT, 2022
** workshop_struct_tek1
** File description:
** init_window
*/

#include "window.h"

/*
* Create a window structure pointer, setting the width the height and the
* renderWindow, and then return it
*
* Don't forget to set the sfRenderWindow framerate to 60
*
* Also use the differents parameters given in the function
* the names are pretty obvious
*/
window_t *init_window(char *window_title, size_t width, size_t height)
{
    window_t *window = NULL;

    /*
    * to Complete
    */
    return window;
}